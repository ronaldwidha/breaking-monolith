'use strict';

exports.recognizePicture = function(imgref, callback) {
    // a very mighty picture recognition algorithm
    return new Promise(function (resolve, reject) {
        resolve(['someone', 'doing something']);
    });
};