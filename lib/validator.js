'use strict';

exports.isSupported = function(metadata) {
    // console.log('validator: isSupported');
    // throw "Not supported image type";
    // return new Promise(function(resolve, reject){
    //     resolve(true)
    // })
    return true;
};